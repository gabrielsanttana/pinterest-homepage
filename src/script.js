// import styles from './App.module.scss';

// const grid = document.querySelector(styles.grid);
// const headingPhrases = document.querySelectorAll(
//   `${styles.heading} ${styles.phrasesWrapper} ${styles.phrase}`,
// );
